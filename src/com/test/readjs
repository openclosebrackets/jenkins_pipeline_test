import groovy.json.JsonSlurperClassic
import com.test 

def statc read_data(String jsonFile)
{
    def json = libraryResource jsonFile                    
    def data = new JsonSlurperClassic().parseText(json)
    return data
}
